/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ServicesLinkedService } from "../servicesLinked.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ServicesLinkedCreateInput } from "./ServicesLinkedCreateInput";
import { ServicesLinkedWhereInput } from "./ServicesLinkedWhereInput";
import { ServicesLinkedWhereUniqueInput } from "./ServicesLinkedWhereUniqueInput";
import { ServicesLinkedFindManyArgs } from "./ServicesLinkedFindManyArgs";
import { ServicesLinkedUpdateInput } from "./ServicesLinkedUpdateInput";
import { ServicesLinked } from "./ServicesLinked";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ServicesLinkedControllerBase {
  constructor(
    protected readonly service: ServicesLinkedService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServicesLinked",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ServicesLinked })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ServicesLinkedCreateInput
  ): Promise<ServicesLinked> {
    return await this.service.create({
      data: data,
      select: {
        backupHost: true,
        drHost: true,
        id: true,
        ip_address: true,
        name: true,
        primaryHost: true,
        silo: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServicesLinked",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [ServicesLinked] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ServicesLinkedFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<ServicesLinked[]> {
    const args = plainToClass(ServicesLinkedFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        backupHost: true,
        drHost: true,
        id: true,
        ip_address: true,
        name: true,
        primaryHost: true,
        silo: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServicesLinked",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ServicesLinked })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ServicesLinkedWhereUniqueInput
  ): Promise<ServicesLinked | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        backupHost: true,
        drHost: true,
        id: true,
        ip_address: true,
        name: true,
        primaryHost: true,
        silo: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServicesLinked",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ServicesLinked })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ServicesLinkedWhereUniqueInput,
    @common.Body() data: ServicesLinkedUpdateInput
  ): Promise<ServicesLinked | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          backupHost: true,
          drHost: true,
          id: true,
          ip_address: true,
          name: true,
          primaryHost: true,
          silo: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "ServicesLinked",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ServicesLinked })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ServicesLinkedWhereUniqueInput
  ): Promise<ServicesLinked | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          backupHost: true,
          drHost: true,
          id: true,
          ip_address: true,
          name: true,
          primaryHost: true,
          silo: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
